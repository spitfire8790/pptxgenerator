# Function enableMapContentEvents

enableMapContentEvents(): void

if called, map content events (e.g. layer added, style updated, etc.) will be dispatched to the SDK app. By default this is enabled.

Returns void