# Function activateLensLayer

activateLensLayer(layerName, overrides?, uiOverrides?): void

Parameters
    layerName: string
    <Optional> overrides: Partial<GiraffeLensableStyle>
    <Optional> uiOverrides: Partial<LensUiState>

Returns void