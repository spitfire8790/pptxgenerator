# Function createProject

createProject(feature, overrides?): Promise<number>

Parameters
    feature: Feature<Geometry, {
        [name: string]: any;
    }>
    <Optional> overrides: ProjectOverrides

Returns Promise<number>